import { UserIndentification } from './user-indentificationmodel';

describe('UserIndentification', () => {
  it('should create an instance', () => {
    expect(new UserIndentification()).toBeTruthy();
  });
});
